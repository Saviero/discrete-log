#include "SmoothGenerator.h"

void SmoothGenerator::generate(FactorBaseGenerator base, ZZ bound)
{
    std::vector<ZZ> rationalSide;
    std::vector<ZZ> algebraicSide;
    for(ZZ b(1); b < bound; ++b)
    {

    }
}